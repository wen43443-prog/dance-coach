# 舞帧 · iPhone 使用指南

## ✅ 好消息：网址已经部署好了，直接用

**你的专属网址：`https://wen43443-prog.github.io/dance-coach/`**

已经发布到你的 GitHub 账号（wen43443-prog）下的 Pages 服务，永久有效、免费、无需电脑。下面"第二步"直接照做即可；"第一步"只需在以后更新版本时参考。

## 第二步：iPhone 上打开（以后每次这样用）

### 方案 A：GitHub Pages（推荐，免费、稳定、无需装软件）

1. 电脑浏览器打开 https://github.com → 点 **Sign up** 注册一个账号（只需邮箱，免费）
2. 登录后，点右上角 **＋** → **New repository** 新建仓库
   - Repository name 填：`dance-coach`
   - 选 **Public**（公开，Pages 要求）
   - 点 **Create repository**
3. 进入仓库页面 → 点 **Add file** → **Upload files**
4. 把 `发布包` 文件夹里的 **5 个文件**（index.html、manifest.webmanifest、sw.js 和 icons 文件夹里的 2 张图）拖进网页，等上传完 → 点 **Commit changes**
   > 提示：直接把 `舞帧-网站发布包.zip` 解压后拖进去即可，注意 index.html 必须在仓库根目录
5. 点顶部 **Settings** → 左侧 **Pages** → Branch 选 `main` → **Save**
6. 等一两分钟，刷新后出现绿色地址栏，形如：
   `https://你的用户名.github.io/dance-coach/`
   **这个网址就是你的 App 地址，永久有效，不再需要电脑。**

### 方案 B：Netlify Drop（更简单，但需要注册 Netlify 账号）

1. 打开 https://app.netlify.com/drop
2. 注册/登录后，把整个 `发布包` 文件夹拖到页面中间
3. 几秒后得到 `https://xxx.netlify.app` 网址

### 以后更新版本

我（或你）改了 `index.html` 后，在 dance-coach 文件夹运行：

```
python 一键发布更新.py push
```

约 1 分钟自动发布（脚本会校验线上内容确实是新版），手机 Safari 刷新即生效。

---

## 第二步：iPhone 上打开（以后每次这样用）

1. iPhone 打开 **Safari**（必须是 Safari，微信里打开不行），输入上面的网址
2. 正常使用：导入视频 → 慢放/逐帧 → 拆解关键帧写口令 → 语音领练
3. **变成 App 图标**：点底部中间的 **分享按钮 ⬆️** → 往下找 **添加到主屏幕** → 点右上角 **添加**
4. 桌面出现「舞帧」图标，以后从图标点开，全屏运行，和 App 没区别
5. 首次打开后 Service Worker 会缓存全部文件，**之后没网也能用**

---

## iOS 注意事项

| 事项 | 说明 |
|---|---|
| 慢放速度 | iOS 建议 0.25× 起步，0.1× 在 iPhone 上可能不够顺滑；逐帧功能不受影响，逐帧是最精确的 |
| 语音 | 首次使用先随便点一下屏幕任意位置（解锁语音）；确保系统有中文语音（iPhone 默认自带） |
| 数据保存 | 视频和关键帧存在手机 Safari 本地，不会上传；**不要用无痕模式**，否则关掉就没 |
| 更新 | 我改了程序后，你把新文件重新上传覆盖，iPhone 上重新打开一次就是新版 |
| 逐帧不准 | 设置里手动改帧率（普通视频 30，高帧率视频 60） |

---

## 常见问题

- **网址打不开/很慢**：GitHub Pages 在国内偶尔较慢，多试几次或换 Netlify；网址是 https 开头才能"添加到主屏幕"后离线使用
- **"添加到主屏幕"里找不到**：往分享菜单最底部找"更多操作"；或点 Safari 底部中间的分享按钮重新找
- **想换手机**：数据在本机浏览器里，新手机需重新导入视频和关键帧
